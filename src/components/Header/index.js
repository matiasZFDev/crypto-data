import './index.css';
import Title from './Title';
import NavLinks from './NavLinks';

const Header = () => {
	return (
		<header id="header">
			<Title />
			<NavLinks />
		</header>
	);
}

export default Header;
